name: Deploy v5 Preview

on:
  push:
    branches:
      - v4

env:
  THEME_NAME: its-theme

permissions:
  contents: read
  deployments: write
  id-token: write
  pages: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-slim
    timeout-minutes: 30
    name: v5 Build
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.SUBMODULES_PAT }}
          submodules: recursive
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Replace password placeholder
        env:
          PASSWORD: ${{ secrets.QUARTZ_PASSWORD }}
        run: find ./content -type f -print0 | xargs -0 sed -i "s/HIDE_FROM_PLAYERS/${PASSWORD}/g"
      - name: Cache dependencies
        uses: actions/cache@v5
        with:
          path: ~/.bun
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lockb') }}
          restore-keys: |
            ${{ runner.os }}-bun-
      - name: Cache Quartz plugins
        uses: actions/cache@v5
        with:
          path: .quartz/plugins
          key: ${{ runner.os }}-plugins-${{ hashFiles('quartz.lock.json') }}
          restore-keys: |
            ${{ runner.os }}-plugins-
      - name: Install dependencies
        run: bun ci
      - name: Install Quartz plugins
        run: bun run -b quartz/bootstrap-cli.mjs plugin restore -v
      - name: Check types and style
        run: bun run check
      - name: Build Quartz
        run: bun run -b quartz/bootstrap-cli.mjs build --bundleInfo -v
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: public

  deploy:
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
